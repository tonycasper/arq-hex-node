import type { Routes, Collections, Core, FilesContents } from "@mocks-server/core";
interface RoutesAndCollections {
    routes: Routes;
    collections: Collections;
}
declare class Plugin {
    static get id(): string;
    private _config;
    private _logger;
    private _alerts;
    private _files;
    private _loadRoutes;
    private _loadCollections;
    private _documentsAlerts;
    private _collectionIdOption;
    private _collectionFromOption;
    constructor({ logger, alerts, mock, files, config }: Core);
    _getRoutesAndCollectionsFromFilesContents(filesContents: FilesContents): Promise<RoutesAndCollections>;
    private get _defaultCollectionOptions();
    _onLoadFiles(filesContents: FilesContents): Promise<void>;
}
export default Plugin;
