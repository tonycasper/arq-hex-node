import type { Routes } from "@mocks-server/core";
import type { OpenApiDefinition } from "./types";
export declare function openApiRoutes(openApiDefinition: OpenApiDefinition.Definition, advancedOptions?: OpenApiDefinition.Options): Promise<Routes>;
